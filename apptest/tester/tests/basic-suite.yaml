actions:
  - name: kubectl smoke test
    bashTest:
      script: kubectl version
      expect:
        exitCode:
          equals: 0
  - name: Wait for deployment get ready
    bashTest:
      script: |
        kubectl get pod ${APP_INSTANCE_NAME}-cron
      expect:
        exitCode:
          equals: 0
  